# This is a template for the configs
API_BASE_URL = (
    "https://example/abc.php"
)
# Fixed part of the query string (everything except the JSON payload)
STATIC_PARAMS = {
    "Command": "COMMAND",
}